set (autostart ${PLUGIN_RIOTCONTROL_AUTOSTART})
set (preconditions Platform)
set (callsign "org.rdk.RIoTControl")

if(PLUGIN_RIOTCONTROL_STARTUPORDER)
set (startuporder ${PLUGIN_RIOTCONTROL_STARTUPORDER})
endif()
